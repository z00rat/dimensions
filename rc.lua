-- location of the repo 
-- use '.' instade of '/'
dimensionsDir = "dimensions".."."

-- inspect for debugging 
inspect = require(dimensionsDir.."inspect")


-- require this file after seting and requireing all other variable and script 
require(dimensionsDir.."dimensionsRc")
